

network net;




int firstNode;
int secondNode;




firstNode = net.addNode(roessler());
secondNode = net.addNode(roessler());

net.addEdge (firstNode,secondNode,  weightedEdge (0.1) );


net.printNodeStatistics();

