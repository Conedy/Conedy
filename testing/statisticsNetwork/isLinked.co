network net;


int source;
int target;

source = net.addNode(node());
target = net.addNode(node());

net.addEdge (source,target, weightedEdge(1.0));

print "Should be 1: " + (net.isLinked (source,target)) + newline;
print "Should be 0: " +(net.isLinked (target,source)) + newline;
