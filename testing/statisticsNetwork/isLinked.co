network net;


int source;
int target;

source = net.addNode(node());
target = net.addNode(node());

net.addEdge (source,target, weightedEdge(1.0));

print "Should be true:" + (net.isLinked (source,target)) + newline;
print "Should be false:" +(net.isLinked (target,source)) + newline;
