network N;


int source;
int target;

source = N.addNode(node());
target = N.addNode(node());

N.addEdge(source, target, weightedEdge(1.0));

print "Should be 1: " + (N.isLinked (source,target)) + newline;
print "Should be 0: " +( N.isLinked (target,source)) + newline;
