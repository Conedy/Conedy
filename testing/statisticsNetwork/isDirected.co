
network net;

net.addNode (kuramoto());
net.addNode (kuramoto());

net.addEdge(0,1, 2.0);

print "Should be true: ";
print net.isDirected();
print newline;

net.addEdge (1,0, 0.1);
print "Should be true: ";
print net.isDirected();
print newline;

net.clear();
net.cycle(10,1, kuramoto());
net.randomizeWeights(uniform(3.0,3.0));
print "Should be false: ";
print net.isDirected();
print newline;


