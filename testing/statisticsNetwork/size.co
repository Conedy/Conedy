network net;

net.lattice(20,20,1.5, roessler());
print "should be 400: ";
print net.size();
print newline;

net.clear();
net.cycle(40,2, kuramoto());
print "should be 40: ";
print net.size();
print newline;
