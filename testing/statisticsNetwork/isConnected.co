network net;

net.addNode(kuramoto());
net.addNode(kuramoto());

net.addEdge(0,1,1.0);
print "should give an error: ";
if( net.isConnected() )
{
print "";
};
print newline;

net.clear();
net.cycle(500,1, kuramoto());



print "should be connected: ";
if( net.isConnected() )
{ 
	print "connected ";
};
print newline;

net.addNode(kuramoto());
print "should be disconnected: ";
if( !net.isConnected() )
{ 
	print "disconnected ";
};
