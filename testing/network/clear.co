
network_edgeVerbosity = 1.0;
network_nodeVerbosity = 1.0;

network n;
n.addVertex<pcoNonleaky>();
print "Should be 1:"+ n.size()+ newline;

n.observe(0,"test");
n.removeObserver();

print "Should be 1:"+ n.size()+ newline;

n.addVertex<pcoNonleaky>();
n.observe(0,"test");

n.clear();

print "Should be 0:"+ n.size()+ newline;

int newNodeNumber;
newNodeNumber = n.addVertex<pcoNonleaky>();
n.observe(newNodeNumber,"test");


n.saveAdjacencyList("output/clear.co.graph");
