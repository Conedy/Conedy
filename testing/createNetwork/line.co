network net;

net.line(1000,1, node(), weightedEdge(1.0));  # Creates a closed chain of 100 nodes where each is connected to its 10 nearest neighbors to each side.


print "should be 1000:" +  net.size() ;
print "should be 1.0:" +  net.meanWeight() ;

print "should be 0.0:" +  net.meanClustering() ) + newline; 
print "should be 333:" +  net.meanPathLength() ) + newline;

print "should be close to 2:" +  net.meanDegree() ) + newline; 
