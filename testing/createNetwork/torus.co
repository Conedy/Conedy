
network net;

net.torus (40,40,1.5, node(), weightedEdge(1.0)); #creates a torus of nodes, where each is connected to its 8 (4 direct, 4 diagonal) nearest neighbors

print "should be 8:" + net.meanDegree()+ newline;
print "should be "+ (12.0/28.0) +":" + net.meanClustering()+ newline;

