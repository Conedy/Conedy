
network net;


net.completeNetwork(10); # adds 10 nodes, which are mutually connected by unweighted edges


print "should be 9:" + net.meanDegree() + newline;
print "should be 1.0:" + net.meanClustering() + newline;
print "should be 1.0:" + net.meanPathLength() + newline;
